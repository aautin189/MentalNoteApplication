

function getNotebook(email){

	// need to return an array of keys (timestamps) in this user's notebook
}


function create(date,title,cta1,cta2,message){

	//check to see if this user's email address is registered in local storage
		//if not, then create
	//ask local storage to add new note to collection, using newly created id as the key
	//return a Note instance to the caller
}


function deleteNote(id){

	//access note using the key (id) provided
	//ask local storage to delete that note object from the collection
}



function getNote(){

	//getNotebook()
}